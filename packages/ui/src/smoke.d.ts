/** Tier 1 smoke checks: every page answers, site pages with a heading, the given pages hydrate cleanly. */
export declare function smokeChecks(options: { sitePaths: string[]; appPaths: string[]; hydrate?: string[]; locales?: string[] }): void;
