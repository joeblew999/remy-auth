/** The demo form's server function and its middleware, in every locale; `path` is the demo page's de-localized path. */
export declare function serverFunctionChecks(options?: { path?: string }): void;
