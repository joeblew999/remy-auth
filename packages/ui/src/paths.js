/** Public paths that exist in every locale; '' is the home page. Routes, sitemaps, entry redirects and the shared checks derive from this list. */
export const publicPaths = ['', '/demo', '/formats'];
