/** The id of the docs search box (live-search.tsx): the header's Search link and ⌘K / Ctrl+K focus it. Its own module, so the header on every page does not load the search box. */
export const docsSearchInputId = 'docs-q';
