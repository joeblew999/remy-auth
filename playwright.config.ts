import { playwrightConfig } from '@joeblew999/remy-ui/playwright';

// The shared Playwright setup: local runs serve the built Worker on Cloudflare's local runtime.
export default playwrightConfig();
