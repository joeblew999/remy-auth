#!/usr/bin/env bash
#MISE description="REMOTE: show or change the AI Gateway in front of this Worker's AI Search: show | rate-limit off | rate-limit <requests>/<seconds> | logs on|off. Changes are read back; run through fnox (CLOUDFLARE_API_TOKEN with AI Gateway Edit). Needs an ai_search binding in the Wrangler configuration (fails on an app without one)"
#MISE dir="{{config_root}}"
# A shell wrapper so the module beside it loads as ESM wherever this directory is included from.
set -euo pipefail
exec node "$(dirname "$0")/gateway.mjs" "$@"
