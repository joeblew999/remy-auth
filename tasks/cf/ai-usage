#!/usr/bin/env bash
#MISE description="REMOTE, read only: AI Gateway usage of this Worker's AI Search: calls, cache hits, failures, tokens, estimated cost and time, per day and per task. Read only; needs Cloudflare login and an ai_search binding in the Wrangler configuration (fails on an app without one)"
#MISE dir="{{config_root}}"
# A shell wrapper so the module beside it loads as ESM wherever this directory is included from.
set -euo pipefail
exec node "$(dirname "$0")/observe.mjs" ai-usage "$@"
